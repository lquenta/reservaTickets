; Fotos integrantes: 1 archivo hasta 5 MB. Subida múltiple: hasta 20 archivos → post_max_size debe ser mayor (ej. 110M).
; Solo aplica si el servidor lee .user.ini (PHP-FPM). Con "php artisan serve" ajusta en php.ini.
upload_max_filesize = 6M
post_max_size = 110M
